name: CD - Deploy API to GKE

on:
  push:
    branches:
      - main
  workflow_dispatch:

# These env vars will pull from the Secrets you just created
env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  GAR_LOCATION: us-central1 # Your AR Region
  REPOSITORY: ${{ secrets.GAR_REPO }}
  IMAGE: iris-api
  CLUSTER_NAME: ${{ secrets.GKE_CLUSTER }}
  CLUSTER_LOCATION: ${{ secrets.GKE_LOCATION }} # Your GKE Zone
  # This must match the name in your deployment.yml
  DEPLOYMENT_NAME: mlops-api-deployment 
  NAMESPACE: default

jobs:
  # ------------------------------------
  # --- PHASE 3 JOB (Already works) ---
  # ------------------------------------
  deploy:
    name: Build, Push, and Deploy to GKE
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout repository
      - name: Checkout
        uses: actions/checkout@v4

      # 2. Authenticate to Google Cloud
      - name: Authenticate Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_CREDENTIALS_JSON }}

      # 3. Configure gcloud CLI
      - name: Configure gcloud CLI
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}
          export_default_credentials: true

      # 4. Install GKE Auth Plugin
      - name: Install GKE Auth Plugin
        run: gcloud components install gke-gcloud-auth-plugin --quiet

      # 5. Configure Docker for Artifact Registry
      - name: Configure Docker auth for Artifact Registry
        run: gcloud auth configure-docker ${GAR_LOCATION}-docker.pkg.dev --quiet

      # 6. Build Docker image
      - name: Build Docker image
        run: |
          # Use GITHUB_SHA to create a unique tag
          export IMAGE_URI="${GAR_LOCATION}-docker.pkg.dev/${PROJECT_ID}/${REPOSITORY}/${IMAGE}:${GITHUB_SHA}"
          echo "📦 Building image: $IMAGE_URI"
          docker build -t "$IMAGE_URI" .
          # Pass this full URI to the next step
          echo "IMAGE_URI=$IMAGE_URI" >> $GITHUB_ENV

      # 7. Push Docker image to Artifact Registry
      - name: Push Docker image
        run: |
          echo "🚀 Pushing image: ${{ env.IMAGE_URI }}"
          docker push "${{ env.IMAGE_URI }}"

      # 8. Get GKE credentials
      - name: Get GKE credentials
        run: |
          gcloud container clusters get-credentials "${CLUSTER_NAME}" \
            --region "${CLUSTER_LOCATION}" \
            --project "${PROJECT_ID}"

      # 9. Deploy to GKE
      - name: Deploy to GKE
        run: |
          echo "🔄 Deploying to GKE with image: ${{ env.IMAGE_URI }}"

          # Replace the placeholder in deployment.yml
          sed -i "s|YOUR_IMAGE_URL_PLACEHOLDER|${{ env.IMAGE_URI }}|g" deployment.yml

          # Apply the deployment and service
          kubectl apply -f deployment.yml

          # Wait for the rollout to finish
          echo "⏳ Waiting for rollout..."
          kubectl rollout status deployment/"$DEPLOYMENT_NAME" --namespace "$NAMESPACE" --timeout=5m

  # ------------------------------------
  # --- PHASE 4 JOB (New part) ---
  # ------------------------------------
  stress-test:
    name: Stress Test Deployed API
    needs: deploy # This job runs ONLY AFTER 'deploy' is successful
    runs-on: ubuntu-latest

    steps:
      - name: Install wrk
        run: |
          sudo apt-get update -y
          sudo apt-get install -y wrk

      - name: Authenticate to GCP (for kubectl)
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_CREDENTIALS_JSON }}

      - name: Get GKE credentials
        run: |
          gcloud container clusters get-credentials "${{ env.CLUSTER_NAME }}" \
            --region "${{ env.CLUSTER_LOCATION }}" \
            --project "${{ env.PROJECT_ID }}"

      - name: Wait for LoadBalancer IP
        run: |
          echo "⏳ Waiting for external IP..."
          TIMEOUT=300 # 5 min timeout
          START_TIME=$(date +%s)
          while [ -z "$EXTERNAL_IP" ]; do
            NOW=$(date +%s)
            if [ $((NOW - START_TIME)) -gt $TIMEOUT ]; then
              echo "❌ Error: Timed out waiting for external IP."
              kubectl get services -n default
              exit 1
            fi
            # This service name MUST match your deployment.yml
            EXTERNAL_IP=$(kubectl get service mlops-api-service -n default -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
            [ -z "$EXTERNAL_IP" ] && echo "Still waiting..." && sleep 10
          done
          echo "✅ Got API IP: $EXTERNAL_IP"
          echo "API_IP=$EXTERNAL_IP" >> $GITHUB_ENV

      - name: Run 1000 request stress test
        run: |
          echo "🚀 Running 1000 request test..."
          wrk -t12 -c1000 -d30s http://${{ env.API_IP }}/
          echo "Watching pods..."
          kubectl get pods -n default

      - name: Run 2000 request stress test
        run: |
          echo "🚀 Running 2000 request test..."
          wrk -t12 -c2000 -d30s http://${{ env.API_IP }}/
          echo "Watching pods..."
          kubectl get pods -n default
